export enum Roles {
  USER= "USER",
  ADMIN = "ADMIN",
  HANDLER = "HANDLER",
  WORKER = "WORKER",
  OWNER = "OWNER"
}

export enum InvoiceRate {
  weekly= "weekly",
  monthly = "monthly",
  yearly = "yearly"
}
